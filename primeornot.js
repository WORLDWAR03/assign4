var readlineSync = require("readline-sync");
let  input = readlineSync.question("Enter a number : ")
let prime = "true";

if(input==1){
    console.log("1 is not a pime or composite number try another one");
}
else if(input>1){
    for(let i=2;i<input;i++){
        
    }
}
